name: Friendly Greetings

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: Greet first-time contributors
      uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: |
          👋 Hi @${{ github.actor }}! 
          This is Yuval`s bot using GITHUB WORKFLOW :)
          Welcome to **${{ github.repository }}** and thanks for opening your first issue!  

          🌟 We're excited to see your ideas and contributions!
        pr-message: |
          🎉 Hi @${{ github.actor }}!  
          This is Yuval`s bot using GITHUB WORKFLOW :)
          Thanks for opening your first pull request in **${{ github.repository }}**!  

          🚀 We’re thrilled to have you contributing. Welcome aboard!
